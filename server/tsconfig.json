{
  "compilerOptions": {
    "target": "ES6",                          /* 변환될 자바스크립트 버전 */
    "module": "commonjs",                    /* Node.js에서 사용하는 모듈 방식 */
    "rootDir": "./",                     /* 실제 코드가 들어갈 폴더 (/*src 밖의 파일도 인식하도록 변경  */
    "outDir": "./dist",                      /* 빌드된 파일이 저장될 폴더 */
    "esModuleInterop": true,                 /* 외부 라이브러리(express 등)를 편하게 불러오게 함 */
    "forceConsistentCasingInFileNames": true,
    "strict": true,                          /* 엄격한 타입 체크 (에러 방지) */
    "skipLibCheck": true,
    "types": ["node"]
  },
  "include": ["src/**/*", "prisma.config.ts"],    /* src 폴더 안의 모든 파일을 검사. prisma 설정 파일도 검사 대상에 포함 */
  "exclude": ["node_modules"]                    /* 라이브러리 폴더는 제외해라 */
}